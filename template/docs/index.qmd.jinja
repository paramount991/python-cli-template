---
title: {{ project_slug }}
subtitle: {{ project_description }}
---

## 简介

`{{ project_slug }}` {{ project_description }}，基于 Typer + pydantic-settings + structlog 构建，开箱即用。

## 核心特性

| 特性 | 说明 |
|------|------|
| **多级 CLI** | Typer 子命令、全局选项、自动 help |
| **配置管理** | TOML 配置文件，环境变量覆盖，pydantic 校验 |
| **结构化日志** | structlog 控制台彩色输出 + 文件 JSON 轮转 |
| **代码质量** | ruff 检查，开箱即用 |
| **测试覆盖** | pytest，覆盖配置、装饰器、全局选项 |
{% if with_example %}| **示例模块** | `{{ example_name }}` 模块，可自行替换为实际业务逻辑 |
{% endif %}
## 快速开始

```bash
# 安装
uv pip install -e .

# 查看帮助
{{ project_slug }} --help

# 初始化配置
{{ project_slug }} config init
{% if with_example %}
# 运行示例业务命令
{{ project_slug }} {{ example_name }} process --help
{% endif %}
```

## 项目架构

```
{{ project_slug }}/
├── src/{{ package_slug }}/
│   ├── cli/              # CLI 命令层
│   │   ├── commands/     # 子命令
│   │   └── callbacks/    # 回调（bootstrap、全局选项）
│   ├── core/             # 核心模块
│   │   └── config/       # 配置管理
{% if with_example %}│   └── {{ example_name }}/             # 示例业务模块
{% endif %}├── tests/                  # pytest 测试
├── docs/                   # Quarto 文档
├── pyproject.toml
└── ruff.toml
```

## 文档导航

- [安装指南](installation.qmd) — 环境搭建与依赖安装
- [使用指南](usage.qmd) — CLI 参考与配置说明
- [API 参考](api.qmd) — Python 模块接口文档
