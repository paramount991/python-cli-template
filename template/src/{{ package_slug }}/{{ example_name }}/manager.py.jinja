"""{{ example_name }} 业务逻辑 (示例存根)."""

from pathlib import Path

from {{ package_slug }}.core.log import get_logger

log = get_logger()


def process(
    input_path: Path | None = None,
    output_path: Path | None = None,
) -> None:
    """执行 {{ example_name }} 业务处理.

    Args:
        input_path: 输入路径
        output_path: 输出路径
    """
    log.warning('process is a stub; implement your business logic here')
    if input_path is not None:
        log.info('input_path=%s', input_path)
    if output_path is not None:
        log.info('output_path=%s', output_path)
